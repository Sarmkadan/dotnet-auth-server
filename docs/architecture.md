# Architecture & Design

This document has moved: see [ARCHITECTURE.md](ARCHITECTURE.md).

The previous version of this file described components that were never part of
the codebase (SQL Server/PostgreSQL data layers, an `AuditLogRepository`, a
standalone API gateway). The current document is written strictly against the
actual code.
